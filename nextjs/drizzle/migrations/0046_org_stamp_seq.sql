-- Общий порядковый счётчик № клейма (партия клейм): менеджер задаёт базу, система
-- предлагает следующий номер; продвигается при сохранении сертификата.
ALTER TABLE org_settings ADD COLUMN IF NOT EXISTS stamp_seq_next integer;
