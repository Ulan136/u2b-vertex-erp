-- Секрет правки заявки из внешнего кабинета (без входа). Возвращается создателю
-- при отправке и хранится у него на устройстве; правка возможна только с токеном
-- и пока заявка не «Готова». Старым заявкам генерируем токен (edit-ссылка была
-- бы не у кого, но пусть колонка непустая для единообразия).
ALTER TABLE orders ADD COLUMN IF NOT EXISTS edit_token uuid DEFAULT gen_random_uuid();
