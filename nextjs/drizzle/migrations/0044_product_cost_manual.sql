-- Ручная себестоимость в приоритете: если админ задал cost_price вручную в карточке
-- товара, приход (закуп) её больше НЕ перезаписывает. Флаг взводится при ручной правке
-- цены и снимается, если админ очистил поле (тогда снова работает авто-цена по приходу).
ALTER TABLE products ADD COLUMN IF NOT EXISTS cost_price_manual boolean DEFAULT false;
