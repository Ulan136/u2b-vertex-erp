import { Card, PageTitle } from '@/components/ui';

export const metadata = { title: 'VERTEX ERP — новый интерфейс' };

// Заглавная нового интерфейса (этап 0). По мере переезда сюда добавляются
// настоящие Next-экраны — навигация слева ведёт в них, а не в старый ERP.
export default function ErpHome() {
  return (
    <div className="erp-welcome">
      <PageTitle title="Новый интерфейс ERP" sub="Переезжаем со старого монолита сюда — по одному экрану за раз" />
      <Card>
        <h3>Этап 0 — каркас готов</h3>
        <p>Это новый, быстрый интерфейс на Next.js. Сейчас здесь только оболочка
          (боковое меню, шапка, права по ролям). Пункты меню со значком <b>↗</b> пока
          открываются в старом интерфейсе — они переедут сюда по очереди.</p>
        <ul>
          <li>Данные и права — те же, что в старом ERP (общий бэкенд и сессия).</li>
          <li>Каждый переехавший экран будет грузиться отдельно и быстрее на телефоне.</li>
          <li>В любой момент можно вернуться в «Старый интерфейс» (ссылка в шапке).</li>
        </ul>
      </Card>
    </div>
  );
}
